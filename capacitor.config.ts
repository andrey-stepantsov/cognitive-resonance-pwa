import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cognitiveResonance.app',
  appName: 'Cognitive Resonance',
  webDir: 'dist'
};

export default config;
